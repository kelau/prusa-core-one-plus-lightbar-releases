
- Combined NTP settings into one comma-separated field supporting up to three servers, preserving existing saved values.
- Added validation for empty entries, malformed hostnames, and excessive server counts.

