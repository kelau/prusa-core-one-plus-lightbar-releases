
- Made animation selectors fixed and compact so color pickers consume the remaining row width, including on mobile.
- Replaced correlated Sparkle flashes with sparse, independently phased per-pixel twinkles that fade smoothly.
- Updated the browser Sparkle preview to show a soft scattered shimmer instead of two stepped moving dots.

