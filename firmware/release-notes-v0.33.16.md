
- Added a USB runtime validation command so the public installer can confirm that the newly flashed application starts correctly.
- Added a serial Plus-button event used by the installer to verify the physical button after installation.

