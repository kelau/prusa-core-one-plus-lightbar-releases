
- Added an optional bearer-token-protected ingress API for controlling lightbar status without PrusaLink.
- Added external state, progress, remaining-time, temperature, target and Z-height inputs, plus explicit override clearing.
- Added an automatic command timeout that safely returns control to PrusaLink if the external controller stops updating.
- Added advanced Network settings for enabling the API, generating its token and configuring its timeout.
- Allowed period-separated labels in the configurable mDNS network name.
- Added live animation previews to every color picker using the selected style, speed and color.
- Rebalanced color controls on narrow mobile screens so the picker and action buttons remain comfortably tappable.

