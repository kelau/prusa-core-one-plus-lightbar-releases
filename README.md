# CORE ONE+ Lightbar firmware releases

This public repository contains compiled firmware binaries only, enabling anonymous over-the-air updates for installed lightbars. Source code, development history, and issue tracking are maintained separately in a private repository.

Firmware binaries are published automatically from the private source build after tests pass. Releases are beta by default and may later be promoted to stable.
